rules_version = '2';

// Cloud Storage is decommissioned for the Firebase Spark zero-billing architecture.
// All storage operations are denied.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
