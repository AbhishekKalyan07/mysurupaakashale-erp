version: '3.8'

services:
  app:
    image: node:23-alpine
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev -- --host 0.0.0.0
    ports:
      - "5173:5173"
