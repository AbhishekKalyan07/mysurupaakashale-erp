/**
 * @deprecated Use `useMyPayments` from `./usePayments` instead.
 * This file is kept as a named re-export only so any remaining import
 * (compiler will flag it as unused) still compiles without error.
 */
export { useMyPayments as usePaymentHistory } from './usePayments';
